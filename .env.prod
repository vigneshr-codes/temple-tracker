# Production Environment Variables
# Copy this file to .env.production on your server and update the values

# MongoDB Configuration
MONGO_ROOT_USERNAME=admin
MONGO_ROOT_PASSWORD=your_strong_mongodb_password_here

# JWT Configuration (Generate a strong secret)
JWT_SECRET=your_jwt_secret_key_at_least_32_characters_long
JWT_EXPIRE=30d
JWT_COOKIE_EXPIRE=30

# Settings Encryption Key (AES-256-GCM for stored API keys/secrets)
# Generate with: openssl rand -hex 32
SETTINGS_ENCRYPTION_KEY=your_64_hex_char_encryption_key_here

# Domain Configuration (Update with your domain)
DOMAIN=your-domain.com

# Email Configuration (Optional - for notifications)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USERNAME=your-email@gmail.com
EMAIL_PASSWORD=your-app-password